#include <stdio.h>

/**
 * main - Entry point
 * Description: Uses the printf function to print a statement.
 * Return: Always 0 (Success)
 */

int main(void)
{
	/* Print the result */
	printf("with proper gramma, but the outcome is a piece of art, \n");

	return (0);
}
